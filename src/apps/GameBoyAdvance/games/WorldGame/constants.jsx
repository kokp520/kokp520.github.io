import { DIRECTIONS } from '../../styles/constants';

// 玩家初始設定
export const INITIAL_PLAYER_STATE = {
  x: 10,
  y: 6,
  direction: DIRECTIONS.DOWN
};

// 地圖設定
export const INITIAL_MAP = 0;

// 攝像頭初始設定
export const INITIAL_CAMERA = {
  x: 0,
  y: 0
};